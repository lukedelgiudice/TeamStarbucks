
construct <- function(file_path, years) {
  teams <- 4 # fix this to parse for teams
  transition <- matrix(0, nrow = teams, ncol = teams)
  
  
  
  
  for (game in 1:games) {
    # get team 1 and team 2
    
    if (p1 > p2) {
      transition[t1][t2] = transition[t1][t2] + 1
    }
    
  }
  
  # normalize
}